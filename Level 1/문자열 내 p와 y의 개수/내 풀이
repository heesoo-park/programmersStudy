def solution(s):
    s_lower = s.lower()
    # 0개도 그냥 count함수로 비교하면 됐었는데 그냥 if문을 추가해버린 코드
    if 'p' not in s_lower and 'y' not in s_lower:
      return True
    else:
      if s_lower.count('p') == s_lower.count('y'):
        return True
      else:
        return False
